import { EntryContentPipe } from './entry-content.pipe';

describe('EntryContentPipe', () => {
  it('create an instance', () => {
    const pipe = new EntryContentPipe();
    expect(pipe).toBeTruthy();
  });
});

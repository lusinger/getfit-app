import { NumericFormatterDirective } from './numeric-formatter.directive';

describe('NumericFormatterDirective', () => {
  it('should create an instance', () => {
    const directive = new NumericFormatterDirective();
    expect(directive).toBeTruthy();
  });
});
